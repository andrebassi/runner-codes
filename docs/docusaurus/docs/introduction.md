---
title: 'Introduction'
description: 'Secure code execution environment for LLMs using Firecracker microVMs'
sidebar_position: 1
hide_title: true
---

![Runner Codes](/img/hero-dark.svg)

## What is Runner Codes?

Runner Codes is a **secure execution environment** for running code snippets generated by Large Language Models (LLMs) inside [Firecracker microVMs](https://firecracker-microvm.github.io/). It provides strong isolation, fast boot times, and support for multiple programming languages.

:::info Open Source

Runner Codes is **fully open source** and available on GitHub. Contributions are welcome!

**GitHub:** [github.com/andrebassi/runner-codes](https://github.com/andrebassi/runner-codes)

:::

## Why Firecracker?

Firecracker is a Virtual Machine Monitor (VMM) developed by AWS that provides:

- **Strong isolation**: Full VM-level isolation, not just container namespaces
- **Minimal footprint**: ~5MB memory overhead per microVM
- **Fast startup**: Boots a VM in ~125ms
- **Security-first design**: Built with a minimal device model and seccomp filters

## Key Features

![Key Features](/img/features-grid.svg)

## Supported Languages

![45+ Supported Languages](/img/languages-grid.svg)

:::tip

See the full [Languages Reference](/languages/overview) for detailed information on each language.

:::

## Quick Example

Here's a simple example of executing Python code in a Firecracker microVM:

```bash title="Execute Python code via CLI"
infra.operator run --lang python --code "print('Hello from Firecracker!')"
```

```bash title="Start API server"
infra.operator api --port 8080
```

```bash title="Execute via HTTP"
curl -X POST http://localhost:8080/api/v1/execute \
  -H "Content-Type: application/json" \
  -d '{"language": "python", "code": "print(\"Hello from Firecracker!\")"}'
```

**Output:**

```json title="JSON data"
{
  "stdout": "Hello from Firecracker!\n",
  "stderr": "",
  "exit_code": 0,
  "load_time": "209ms",
  "exec_time": "45ms"
}
```

## Next Steps

- [Quickstart Guide](/quickstart) - Get started in 5 minutes
- [Architecture](/architecture) - Learn how it works
- [CLI Reference](/cli/overview) - Command reference
- [API Reference](/api-reference/overview) - HTTP API documentation
